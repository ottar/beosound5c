# Local deploy target override (see my-hosts.env.example)
my-hosts.env

# Python
.venv/
.venv
__pycache__/
*.pyc
sonos_env/

# macOS
.DS_Store
._*

# Per-device state (generated on the device, not source)
device_id

# On-device artwork cache
web/assets/cd-cache/

# Cloudflare Wrangler cache
.wrangler/

# Credentials and tokens (NEVER commit these)
.spotify_token_cache
*.token
*.cache
config.env
secrets.env
services/sources/*/*.json

# Logs
*.log
fetch.log

# IDE
.idea/
.vscode/
*.swp
*.swo

# Project instructions (keep locally, exclude from public repo)
publish.sh

# Environment-specific
/etc/beosound5c/
.claude/

# Device-specific config (personal IPs, MACs, scenes)
config/church.json
config/kitchen.json
config/office.json
web/json/config.json

# Generated playlist data and digit mappings (written by fetch.py per-device)
web/json/*_playlists.json
web/json/*_digit_playlists.json
web/json/playlists_with_tracks.json
